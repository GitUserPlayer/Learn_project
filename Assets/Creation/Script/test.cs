using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{

    [Range(0,9)]public float AA;
    [Header("NAME")]
    public string Name;
    [Tooltip("ID")]
    public int ID;
    public string SSSS;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
